<%= form_with(model: datatable, local: true) do |form| %>
  <% if datatable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(datatable.errors.count, "error") %> prohibited this datatable from being saved:</h2>

      <ul>
      <% datatable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
